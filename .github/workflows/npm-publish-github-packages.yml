
name: test

on:

  pull_request:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:5.7
        env:
          MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
          MYSQL_ROOT_HOST: '%'
          MYSQL_DATABASE: ${{ secrets.DB_DATABASE }}
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=5

    steps:

      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Create Directory
        run: |
          sudo mkdir -p /opt/madhura_kurhadkar_002769373_05
          sudo chmod 777 /opt/madhura_kurhadkar_002769373_05
        shell: bash

      - name: Set up Environment Variables
        run: |
          echo "PORT=${{ secrets.PORT }}" > /opt/madhura_kurhadkar_002769373_05/.env
          echo "DB_HOST=${{ secrets.DB_HOST }}" >> /opt/madhura_kurhadkar_002769373_05/.env
          echo "DB_USERNAME=${{ secrets.DB_USERNAME }}" >> /opt/madhura_kurhadkar_002769373_05/.env
          echo "DB_PASSWORD=${{ secrets.DB_PASSWORD }}" >> /opt/madhura_kurhadkar_002769373_05/.env
          echo "DB_NAME=${{ secrets.DB_NAME }}" >> /opt/madhura_kurhadkar_002769373_05/.env
          echo "Contents of .env file:"
          cat /opt/madhura_kurhadkar_002769373_05/.env
          echo "List files in the current directory:"
          sudo ls -al
          echo "List files in GitHub workspace:"
          sudo ls -a ${{ github.workspace }}
        shell: bash
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Install Dependencies
        run: npm install

      - name: Run Integration Tests
        run: npm run test

